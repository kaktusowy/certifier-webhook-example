# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @certifier-webhook/backend`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t certifier-webhook-backend`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system certifier-webhook-backend && \
    adduser --system -G certifier-webhook-backend certifier-webhook-backend

COPY apps/backend/dist certifier-webhook-backend/
COPY apps/backend/package.json certifier-webhook-backend/
RUN chown -R certifier-webhook-backend:certifier-webhook-backend .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix certifier-webhook-backend --omit=dev -f install

CMD [ "node", "certifier-webhook-backend" ]
